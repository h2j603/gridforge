import { PanelSection } from "./PanelSection";

export function GridPanel() {
  return (
    <PanelSection title="Grid">
      <p className="text-xs text-ink-soft">
        Columnar / modular / manuscript / custom — coming in v0.2.
      </p>
    </PanelSection>
  );
}
